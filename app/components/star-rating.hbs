{{#if (has-block)}}
  {{yield this.stars}}
{{else}}
  {{#each this.stars as |star|}}
    <FaIcon
      @icon="star"
      {{!-- fas (solid style), far (regular style) --}}
      @prefix={{if star.full "fas" "far"}}
    />
  {{/each}}
{{/if}}
